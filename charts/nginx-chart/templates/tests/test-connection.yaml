apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mginx-chart.fullname" . }}-test-connection"
  labels:
{{ include "mginx-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mginx-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
